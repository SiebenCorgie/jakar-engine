#version 450


//This shader is only used to finalyse the depth pre rendering for the light
//compute shader
//layout(location = 0) out vec4 FragColor;

void main() {
  //don't do anything, we have no buffer to write on
  //FragColor = vec4(1.0);
}
